name: build

on:
  push:
    paths-ignore:
      - "**/*.md"

env:
  NAME: debug

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@v22

      - name: Build Containers
        id: build-containers
        shell: bash
        run: |
          nix build --out-link result-container && docker load < result-container

      - id: "auth"
        name: "Authenticate to Google Cloud"
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider:  ${{ secrets.NAIS_IO_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: gh-${{env.NAME}}@nais-io.iam.gserviceaccount.com
          token_format: access_token

      - name: "Handle auth failure"
        if: ${{ failure() && steps.auth.outcome == 'failure' }}
        shell: bash
        run: |
          cat <<EOF
          ::error ::Failed to authenticate to Google Cloud.
          EOF

      - name: "Login to registry"
        id: login-to-registry
        uses: docker/login-action@v4
        with:
          registry: "europe-north1-docker.pkg.dev"
          username: "oauth2accesstoken"
          password: "${{ steps.auth.outputs.access_token }}"

      - name: Push image to registry
        id: push
        shell: bash
        run: |
          docker push europe-north1-docker.pkg.dev/nais-io/nais/images/${{env.NAME}}:latest

      - name: Install nix2sbom
        uses: EricCrosson/install-github-release-binary@2e81f4af531bbe08af1e153c8e0e2ccf8f08504e # v2.5.20
        with:
          targets: louib/nix2sbom/nix2sbom@v2.8.3

      - name: Generate the SPDX manifest
        run: |
          nix2sbom "$(readlink -f result-container)" > sbom.json
          cat sbom.json
